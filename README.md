# libdsa
```bash
# This is a C/C++ pre-built dynamic library including some Data structures and Algorithms in C which will be written in a modern, generic and modularized manners.

## Common

```bash
